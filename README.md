# kiran-utils

A small TypeScript utility library containing reusable functions for date formatting and math operations.

## Functions

### formatDate(date)
Formats a JS date into `dd-mm-yyyy`.

### add(a, b)
Returns the sum of two numbers.

### multiply(a, b)
Returns the multiplication of two numbers.
